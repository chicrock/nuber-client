import React from "react";
import styled from "../../typed-components";

const Container = styled.div``;

const RidePresenter: React.SFC = () => {
  return <Container>Ride</Container>;
};

export default RidePresenter;
