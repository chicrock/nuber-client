import React from "react";
import styled from "../../typed-components";

const Container = styled.div``;

const ChatPresenter: React.SFC = () => {
  return <Container>Chat Container</Container>;
};

export default ChatPresenter;
